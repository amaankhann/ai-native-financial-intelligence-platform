# API Reference

- GET /health
- POST /api/intelligence/run — execute one named engine.
- POST /api/intelligence/auto — detect triggers, apply data-quality gates, activate specialist engines and reconcile outputs.
- POST /api/scenarios/run — apply numeric shocks, re-evaluate triggers and execute activated engines.

These are reference architecture endpoints, not investment advice or production trading models.
